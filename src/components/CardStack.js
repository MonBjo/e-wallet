import './CardStack.css';

import Card from './Card';

function CardStack() {
  return (
    <section className="cards">
      < Card />
      < Card />
      < Card />
    </section>
  );
}

export default CardStack;